//Write a C program to take an integer number(x) as input and print “Non-negative” if x>=0. Otherwise print “Negative” without quotation.

#include <stdio.h>
#include <stdlib.h>

int main()
{   printf("\t\t\t\t\"Misbah Ahmed Araf's Statement problems\"\n\n\n");

    int x ;
scanf("%d",&x);

if (x<0)
printf("%d is a negative number",x);

if (x>=0)
printf("%d is a non-negative number",x);



    return 0;
}
